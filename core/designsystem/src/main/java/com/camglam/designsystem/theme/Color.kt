package com.camglam.designsystem.theme

import androidx.compose.ui.graphics.Color

// Paleta "editorial-noir" — CamGlam
// Fondos y superficies (dark default)
object EditorialNoir {
    val Background = Color(0xFF0D0D0D)
    val Surface = Color(0xFF161616)
    val SurfaceHigh = Color(0xFF1E1E1E)
    val Outline = Color(0xFF2C2C2C)

    // Texto
    val InkWarm = Color(0xFFF5F1EA)   // ivory
    val InkMuted = Color(0xFF9E9E96)

    // Acentos (rojo VOGUE + dorado highlights)
    val EditorialRed = Color(0xFFE4001B)
    val Gold = Color(0xFFC9A227)

    // Semánticos (reglas ui-ux-design)
    val Success = Color(0xFF2FBF71)
    val Warning = Color(0xFFF2A541)
    val Error = Color(0xFFE5484D)
}

// Modo claro
object EditorialNoirLight {
    val Background = Color(0xFFFFFFFF)
    val Surface = Color(0xFFFAF8F5)
    val SurfaceHigh = Color(0xFFF0EDE7)
    val Outline = Color(0xFFE2DED6)
    val InkWarm = Color(0xFF111111)
    val InkMuted = Color(0xFF6A675F)
    val EditorialRed = Color(0xFFD30018)
    val Gold = Color(0xFFA8861E)
}